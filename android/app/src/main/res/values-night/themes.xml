<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark theme variant — NoActionBar since Compose manages the UI chrome. -->
    <style name="Theme.DittoEdgeStudio" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/ditto_background_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
