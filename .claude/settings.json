{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r \".tool_input.command // empty\"); if printf \"%s\" \"$cmd\" | grep -qE \"pm[[:space:]]+(uninstall|clear)|adb([[:space:]]+-s[[:space:]]+[^[:space:]]+)?[[:space:]]+uninstall|gradlew[^|;&]*uninstall\"; then printf \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PreToolUse\\\",\\\"permissionDecision\\\":\\\"deny\\\",\\\"permissionDecisionReason\\\":\\\"Blocked: destructive adb/gradle command (uninstall / pm clear) wipes app data. See plans/android/config-loss-investigation.md. Use ANDROID_SERIAL=<serial> with installDebug instead.\\\"}}\"; elif printf \"%s\" \"$cmd\" | grep -qE \"gradlew[^|;&]*connected[A-Za-z]*AndroidTest\" && ! printf \"%s\" \"$cmd\" | grep -q \"58300DLCR0000L\"; then printf \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PreToolUse\\\",\\\"permissionDecision\\\":\\\"deny\\\",\\\"permissionDecisionReason\\\":\\\"Blocked: connectedAndroidTest uninstalls the app and wipes its data. Only allowed on the wipe-safe Pixel 10a: prefix with ANDROID_SERIAL=58300DLCR0000L.\\\"}}\"; fi",
            "timeout": 10,
            "statusMessage": "Checking for destructive device commands"
          }
        ]
      }
    ]
  }
}