baseten.sh
build/
SwiftUI/build
SwiftUI/Ditto Edge Studio.xcodeproj/project.xcworkspace/xcuserdata
.DS_Store
SwiftUI/Ditto Edge Studio/dittoConfig.plist
SwiftUI/EdgeStudio/testDatabaseConfig.plist
SwiftUI/scripts/*.dmg
SwiftUI/scripts/*.dmg.zip
rust/.idea
rust/target
.env
rust/.vscode
rust/debug
rust/designs
.claude/*
!.claude/settings.json
.codex/*
!.codex/config.toml
fontawesome
.periphery_baseline.json
.worktrees

# Test coverage reports
SwiftUI/TestResults.xcresult
SwiftUI/coverage.json
SwiftUI/coverage.txt
SwiftUI/coverage_files.txt
android/.idea
android/.kotlin
*.dmg

# Help docs — generated at build time from docs/help/
# Source of truth is docs/help/*.md — run scripts/sync-help-docs.sh to populate
android/app/src/main/assets/help/
.gitnexus
docs/superpowers

# Screenshots — used only for ad-hoc issue reports / iteration aids,
# never meant to be checked in. Anchored to the repo root so a
# `screens/` directory nested elsewhere isn't accidentally ignored.
/screens/

# Local databases and device backups — never checked in.
#
# These are scratch artifacts: a `data/dto.db` from ad-hoc SQLite poking, and
# `adb backup` archives. The app's real stores live in its sandbox container
# (~/Library/Containers/<bundle-id>/Data/Library/Application Support/), never in
# the repo. This matters beyond tidiness: the local configuration store is
# plaintext (see docs/CREDENTIAL_STORAGE.md), so a committed database file would
# publish whatever database tokens, license tokens and API keys it happened to
# hold. No tracked file has ever matched these patterns.
/data/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.ab

# .NET build output — MSBuild writes intermediates to obj/ and binaries to
# bin/ beside every .csproj. Scoped to dotnet/ rather than a bare `bin/` so
# an unrelated directory of that name elsewhere in the tree isn't caught.
dotnet/**/bin/
dotnet/**/obj/
