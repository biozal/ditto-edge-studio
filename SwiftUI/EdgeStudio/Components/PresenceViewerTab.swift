import DittoSwift
import SwiftUI

struct PresenceViewerTab: View {
    var body: some View {
        // Use custom SpriteKit presence viewer with scroll wheel zoom support
        PresenceViewerSK()
    }
}
