Go into plan mode.  Today we have the queryDetailView function in the DetailsView.swift file that shows the "Query Editor and Results" screen.  The queryTabInspectorView function in the InspectorsView.swift file renders the inspectors that show up on the right side of the screen.  You can see this in the screenshot queryDetailView.png.

I would like to add a new tab Query Metrics which shows the query metrics for the last ran query.  We already track this feature for the Query Metrics screen that shows off the last queries that were ran.  You can see this in queryMetrics.png.  This code lives in the QueryMetricsDetailView.swift file in the recordDetail function.  

To start out we would need to check to see if metrics are enabled in the Settings of the app, if they are enabled, then we would add an icon to the picker.  We already have the Query Metrics icon in the main menu listing of features in the Sidebar and we can reuse that same icon.  

If no query has been ran this view should just show a message that says no Query has been executed or something like that. Make the message look good and it should be veritcally and horizntialy centered.

If a query has been ran then it should show the same UI as on the Query Metrics page from the recordDetail function.

This feature needs to work on all platforms and for smaller screens it will probably need to be scrollable.  

Make a plan on how we can implement tis feature using our existing code and patterns without breaking our existing pages and functionality.  Let me know what the plan is so I can review and decide what to do next.  Use XCode MCP for all code discovery.  