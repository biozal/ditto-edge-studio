Go into plan mode.  You gave me this message from our last session:
  - memory/peer-transport-investigation.md — full context file with:                                                                                         
    - The problem description and current live app state (4 peers, BLE+LAN disabled, AWDL+Cloud on)
    - All 4 code fixes already applied (with file paths and what changed)                                                                                    
    - Exact get_app_logs / get_ditto_logs calls to run in the new session
    - The diagnostic decision tree (SDK bug vs our code)
    - The updated MCP tool count (now 15)
  - MEMORY.md — updated with a pointer to the investigation file and the corrected tool count note

  In the new session, the first thing to do is run get_app_logs(filter: "[Presence]", lines: 500) and look for [Presence] Raw DittoConnectionType:
  'bluetoothLE' entries while BLE is disabled — that's the smoking gun for the SDK bug.
  