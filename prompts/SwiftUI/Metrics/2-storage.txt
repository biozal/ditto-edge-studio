Go into planning mode.  All code discovery should be done using XCode MCP for the Ditto Edge Studio code.  I would like to add a new feature to the app.  This feature would display storage calculations for each collection in a given database.  Another team has built an application that calculates this. I would like you to review the code in:

~/Developer/ditto/support-tooling-ditto-apps/iOS/DittoStore_Calculator/

This app displays the following information:
Collection payload(JSON)
SQLite WAL/SHM
Logging
Metadata & other overheads
Total on-disk (all Ditto)

These are sizes in MB.  A screenshot of the app calculating a single collection is storage.png.  

We can also see a breakdown of storage from __small_peer_info using this query:

SELECT device_disk_usage FROM __small_peer_info

An example of the output is:
{
  "device_disk_usage" : {
    "device_available" : 31548033820,
    "device_total" : 994662584320,
    "ditto_attachments" : 1149350,
    "ditto_auth" : 9265,
    "ditto_replication" : 3612811,
    "ditto_store" : 2434104,
    "ditto_total" : 25667876
  }
}

I would like to display information from both the calculator code and __small_peer_info in Megabytes.  __small_peer_info returns all numbers in bytes.  I would like these to show up in App Metrics in a new section called Storage.  I believe we have App Metrics turned off on iOS (iPad and iOS).  I would like to turn it back on in those platforms so they can see storage information.  

Please create a plan to figure out how we can show off the following information of how much storage it takes in cards like the current App Metrics info:

Collection payload(JSON) (calculator code)
SQLite WAL/SHM (calculator code)
Logging (calculator code)
Metadata & other overheads (calculator  code)

Attachments (__small_peer_info - ditto_attachments)
Auth (__small_peer_info - ditto_auth)
Replication (__small_peer_info - ditto_replication)
Store (__small_peer_info - ditto_store)

Total  (_small_peer_info - ditto_total)

Once the plan is completed let me know so I can review it.  As part of this please make sure you have in the plan on how to write tests for this functionality.