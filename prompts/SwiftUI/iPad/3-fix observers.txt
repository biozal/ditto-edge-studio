Go into plan mode.  We need to fix several things going on with observers on iPad and iPhone.  

What I'm about to describe is in the observers sidebar - which renders observeSidebarView function in the SidebarViews.swift file.  

Once you create an observer - we show it in a card.  This card has the ability to swipe the card from the right to the left in order to either activate/stop the observer, or to delete it.  

The problem is that the space in the sidebar is too small and really the activate/stop button become the only buttons visable.  You can see this in the ipad-observer-delete.png and ipad-observer-final-button.png.  I was able to get this screenshot by holding my finger on the screen during the swipe action to see the animation so I know both buttons show up in the animation.  

The fix for this is to to move delete to a swipe from left to right action.  This should work on both iPad and iPhone.  Since we are moving destructive stuff to the swipe left to right, we should do this for subscriptions which is rendered in the subscriptionSidebarView function of the SidebarViews.swift file.

The second problem we have is once an observer is activated, events show up in the details side of the screen.  This is rendered in observeDetailView function of the DetailViews.swift file.  This renders a list of observer in the view observableEventsList.  When I select a row in this list - the background color and the text color are too similar and I can't read it. This happens on both iPhone and iPad.  On MacOS it's hard to read, but I can understand what the text it.  On selection can we make the background color the ditto yello color and then the text color black so this is readable?  

Make a plan on how to do these changes and then let me know when it's ready for me to review.  Use XCode MCP for all research.