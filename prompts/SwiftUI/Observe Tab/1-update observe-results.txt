Go into plan mode.  I want to talk about how the details Observe screen shows the data to the end user.  This code is in the DetailViews.swift file and starts in the observableDetailSelectedEvent.

Today they get a picker between three options:
Items, Inserted, Updated

Below this choice is a list of JSON data showing the raw data that was returned.  This reuses the ResultJsonViewer component that QueryResults uses.  Below this is pagination controls.  Finally the :details section the DetailBottomBar component renders, which can be found in the Components folder.  

We have a few issues - to start out with we are rendering pagination controls twice.  We render them in the observableDetailSelectedEvent and we also render them in DetailBottomBar.  We want this to work like QueryResults where the pagination is done in DetailBottomBar.  You can see how we did this in the queryDetailView function in the DetailViews.swift file.  

On top of this we would like to display the data in either Raw or Table view like we do in queryDetailView.  However, we still want to allow the user to filter the data by Items, Inserted, and Updated.  What I would like to have is the Raw and Table options for rendering data with a filter button that is justifed right of the Raw/Table picker that is a Picker drop down where they can select which filter they want to use.  It would default to Items by default.  

Because this now introduces features from queryDetailView we would want the ability to have the observeDetailInspectorView inspector to show both the jsonInspectorContent and the HelpContentView via a menu like is done in queryTabInspectorView and jsonInspectorContent should be listed first in this menu and should work like it does with queryTabInspectorView.

Make a plan on how we can do this update.
