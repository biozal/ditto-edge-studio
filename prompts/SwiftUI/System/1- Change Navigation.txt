Go into Plan mode.  Use XCode MCP for all code discovery.  Currently today a Picker is rendered with navigaion menu items (line 84 of the MainStudioView.swift).  This renders 4 icons that are created in the ViewModel buildSidebarItems function.  I would like to change this out from being a Picker to the UI in Apple music where each icon is listed out and the the name of it.  See the screenshot named apple-music.png.  

You can see it has the menu items Search, Home, New, and Radio.  I would like my menu to look like this with the same font size and icon sizes for:

Subscriptions
Query 
Observers 
Metrics

It should use the same icons that already defined in the buildSidebarItems function I just want them to render as a menu like in Apple music.

I also want to change how the sidebars are rendered.  Currently today each screen renders it's own sidebar - so there is a separate sidebar for subscriptions, query, observers, and metrics.  Moving forward I would like to just use one sidebar.  How this will work is that it will work like Apple Music where the content will be in sections. I would like section titles to render like they do in Apple music left justifed with the same font and size that Apple Music uses.

The first section - Subscriptions today uses cards.  I would like to switch this out to use a tree interface like we did with Collections.  You can look at the collectionsSidebarView function in the SidebarViews.swift file for the example on how we did it there.  The root of the tree will be the subscription name.  The icon for it will be arrow.trianglehead.2.clockwise.rotate.90.  The left of the node will be the DQL query which will use the note.text icon.  The header of this section should have some padding after the name and then show the QR Code button.  

The second section - Collections will use the existing UI from collectionsSidebarView in the SidebarViews file.  The section title should be "Collections" and the refresh icon should be next to it with just a little bit of padding so it looks nice.  The icon should follow the style of the header so I think it should be the same color as the text of the header without the circle border.  All existing functionality should still work like clicking on a colleciton name puts a select query in the query editor and navigates to the query editor detail view.  

The third section should be observers.  This should be changed out like subscriptions and should work like subscriptions where the root of the node is the the name of the observer and when you open up the tree the node should be the SQL query.  It should also use the note.text icon before the query.  

The forth section should be Metrics.  That should just have two icons under it - one for App and one for Query and they should work like they do today where it calls up the Metrics detail and routes to the proper view for showing the App metrics or query metrics.  One change will be on iOS to not show the App metrics since the Swift Metrics package only really works on MacOS.  

The insert button should now have all options for inserting in the menu regardless of which detail screen you are on.  This means the menu should have:

Add Subscription
Add Observer
Add Index

Divider
Import Subscriptions -> QR code
Import from Server (this should be renamed to Import Subscriptions -> Server)

The Sidebar will need to be scrollable for smaller screens and Apple Music currently also provides scrolling.  Please research the best way to handle these updates using the Apple Human Interface Guidelines to make sure we are following best practices:  

https://developer.apple.com/design/human-interface-guidelines

Naviation and all existing functionality must continue to work as it does today after this change.


We should also make sure we are putting code in the proper locations based on best practices on where we currently have code (updates to sidebar should be in the SidebarViews.swift file, etc)

Create a plan and let me know when you are ready for me to review it.  

