Go into planning mode.  We need to make changes to the dotnet version of the app.  The navigation bar has an option for Query, which brings up the UI for the QueryListingView and the QueryView.  I have a screenshot of the current UI which is collections-dotnet.png.  The UI needs several changes.

1.  To start out the inspector content is duplicated.  If you look at the screenshot you can see this.

2.  The inspector should use icons instead of words for the tabs.  It takes up less space and then matches the SwiftUI interface.  There also needs to be the following tabs:

- Query History 
- Favorites
- JSON Viewer
- Query Explain
- Query Help

You can inspect the SwiftUI code for what the icon names are.  The SwiftUI inspector code is in the InspectorViews.swift file.  

3.   The colors of the Query editor and the results don't match the rest of the app.  Fix the colors so that it uses the new theme colors.

4.  The Query Results should only have two tabs - JSON and Table.  Currently it has 3:  JSON, Table, Explain.  Remove the Explain tab.

The Execute Query button is on the bottom of the query window.  It should be at the top above the editor that lets you edit a Query.  Next to the button needs to be a pull down box that allows you to select between "Local" and "HTTP".  The default should be local and HTTP should only be an option if the user provides a httpApiUrl AND httpApiKey in the DittoConfigForDatabse.  If those are not set, then we can't call the HTTP API so it should be removed.

5. The code file break down should more match SwiftUI.  How SwiftUI breaks things down is:

The DetailViews.swift file has a function called:  queryDetailView
function and then it calls the two components:

- QueryEditorView
- QueryResultsView

The QueryEditorView draws the query editor and the QueryResultsView draws the tabs for JSON and Table.  

6.  When the QueryEditor is rendered the DetailBottomBar rendering changes to include pagnation controls in the SwiftUI Version.  The dotnet version should do the same thing.  There should only ever be 1 DetailBottomBar in the UI.  

7.  We need to create a new Floating Action button.  it should be located in the lower left hand corner of the Sidebar of any open view.  When it's selected it should have the same options as the SwfitUI version.  You can see a screenshots  

- fab-swiftui.png 
- fab-menu-swiftui.png

The menu when the button is clicked on should contain:

- Add Subscription
- Add Observer
- Add Index
- Divider
- Import Subscriptions -> QR code
- Import Subscriptions -> Server
- Divider
- Import JSON data

For this work this should replace the current button add in a subscription and should bring up the ui for adding a subscription when it's selected.

8.  When Query is selected the QueryListingView should have a dynamic UI that draws a list of collections.  This is Tree View.  At the root of the tree view should be the collection name that is left justified and badge with the count of documents justified on the right.  The badge should have a yellow background with black text.  If the collection has any indexes, a cheveron should allow the collection tree to be open and then each index name should be listed.  The index should allow to be open and it should show the field tha the index uses.  You can review the SwiftUI code to see how this works in the collectionTreeRows function in the SidebarViews.swift file. 


9.  The structure of the code is just throwing all the code in a single folder and as the app is growing it's hard to find things.  The SwiftUI folder structure is much better, which has the following structure:

Views
 - Database (code involving listing databases and the editor)
 - Logging (the code detailing with logging)
 - Metrics (the code deailing with metrics)
 - Settings (the codde detailing with the app preferences)
 - StudioView - code that details with the ui rendering once a database is selected
 - Details - any code that is part of the "Details" views

All the inspector code is in a file called InspectorViews.swift and all the code dealing with Side Bars are in the file SidbarViews.swift.  

A reminder that the app in Dotnet is based on the following once a database is selected:
- NavigationBar (all views see this)
- Sidebar (Sync, Query, Observers, and Query Metrics use a sidebar).  Logging and App Metrics do NOT use a sidebar.
- DetailView - the actual content of each view
- Inspector - hidden by default but draws optional overflow views like help and for some screens like Query optioal functionality.


As part of this update we need to refactor the code in the dotnet app to be more structured in the views so we can find things faster.  

You need to make sure tests exist to make sure everything keeps working.  A build should be done and there should be manual testing that I can do to validate that the app works as part of the process.  We might need to iterate a few times to get the app updated properly.  

Make a plan to do all these changes.  Let me know when the plan is ready for review.  


