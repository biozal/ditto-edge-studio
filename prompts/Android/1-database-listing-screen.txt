Go into planning mode.  Use the MCP Server connection http://127.0.0.1:64342/sse for Android Studio and Xcode MCP for Xcode Swift access.

Read up on the latest version of Material Design here:
https://m3.material.io/


We are working on the Android version of the app.  The android version is in the android folder.  There is a Claude.md file in that folder you can read in to learn about the project.  I want all UI for Android to use the latest M3 version of Material design so it looks like a modern Android application.

I'm working on converting my SwiftUI application to Android.  The android folder has the Android app and the SwiftUI folder has the SwiftUI app.  I'm going slow in the conversion so I can test each feature as we convert it.

I would like to conver the Database Listing and adding database configuration to the database.  The UI is different between mobile phones with small screens and larger tablets with bigger screens.  

I'll start with the phone version.  Screenshots of the iPhone version can be found in the screens\android folder.  The default view of the app when it's first launched it's called database-listing-no-items-phone for dark mode and database-listing-no-items-phone-light.png for light mode.  

You can see that on these screen there are three buttons:

The two at the top are import from QR Code and a Cloud icon.  Both are buttons using Apple's Liquid Glass UI on a Toolbar.  As part of converting the UI to Android add the buttons to the Android app but the QR Code import we can work on in a future feature.  The cloud icon button when tapped opens a web browser and browses to the Ditto portal at:  https://portal.ditto.live/

The button in the lower right hand corner is the add configuration button and when it is clicked it brings up a form to add a new Database Configuration to the database.

The default state when no database configurations are found in the database a message appears "No Database" - "Tap + to add a database configuration".  This message in Android should state "Tap + to register a database configuration".

The code for this in SwiftUI is in the Views\Database folder named DatabaseList.swift. 

When a user clicks on the + button, a new form appears which in SwiftUI is the DatabaseEditorView.swift file in the same folder.  This brings up a full screen action sheet.  The top of the screen has a segment control that allows the user to select one of two forms:  Server and Small Peer Only.  This is mapped to the Database Configuration "mode" field.  We use this to calcualte how to initialize the Ditto instance.  

In android I believe the segement control maps to the secondary tab control in material design:
https://m3.material.io/components/tabs/overview

The form on iPhone is attractive.  The Server form can be seen in these screenshots:

register-database-server-phone-light.png
register-database-server-phone.png

The Small Peer Only form can be seen in these screenshots:
regsiter-datbase-smallpeer-phone-light.png
regsiter-datbase-smallpeer-phone.png

When the user registers a database connection - they can set the "default" log level for Ditto.  This is a Picker which pulls up a listing - which you can see in this screenshot:
register-database-server-log-level-phone-light.png
register-database-server-log-level-phone.png

Once a user taps the Save button it's saved to the database and the Database Listing screen appears again.  Databases Configs are drawn on cards.  These cards are attractive - you can see a screenshot of these here:
database-listing-phone.png
database-listing-phone-light.png

The cards support long press which pops up a menu with action items - you can see this in the screenshot:
database-listing-menu-phone-light.png
database-listing-menu-phone.png

The delete icon obviously deletes the item from the database and redraws the screen.  Scan QR Code we can handle in a future feature.  The edit option brings up the same form for registering a database config but allows the user to edit the existing database config.  Saving it updates the database with the changes in the config.

I would like the UI in Android to match IOS as close as possible, but using the latest design patterns in Material Design.

I would note in iPhone and MacOS I'm using Ditto SDK version 4.13.3 which requires websocket Url.  In Android we are going to use the 5.0 version of the SDK which no longer requires this field.  

In tablet and MacOS (larger screen) the UI for the database listing looks a lot different.  You can see a screenshot of this in database-listing-macos.png.  There is an image in the background.  In SwiftUI this is a PDF so that is scales - but I made that PDF from an SVG file which I have.  I'm not sure which image format Android would need to scale the background image to take up the entire window regardless of the large screen size.  I will need you to reearch that and let me know.

The database cards are in a semi-tranparent list you can see the image through the listing.  I like this UI and it looks very attractive. I would like larger screen Android devices (tablets, laptops) to draw the UI like this.  

When a user taps on a database card it loads the main UI of the application which in SwiftUI is called MainStudioView.  I would like the same view.  For now just make a new View that draws the word MainStudioView when the user taps on it.  

Make a plan to implement all this.  Use the Swift code for guidence on how to write the Android code.  As part of this I'll need you to research the background image format you will need and where I need to put the image so it renders properly.  

As part of the plan we need tests to validate all this works.  Let me know when you have the plan ready for my review.   