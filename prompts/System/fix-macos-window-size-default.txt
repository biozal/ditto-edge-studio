Go into plan mode.  Use Xcode MCP for all discovery of the code.  There is an issue when the MacOS window is resized to it's smaller size the UI is no longe useable as the side bar and inspectors can't be interacted with and buttons disappear.  See these two screenshots:

macos-small.png
macos-normal.png

Because of this we need to make sure that we hard code the smallest size possible on the window to make it this sizing:

1400 x 900

This will allow for the UI to continue to function properly.  Will this cause any issues with any modern apple hardware starting with the M1 series?  

Make a plan for this and let me know what you find out.
